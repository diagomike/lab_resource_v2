import ComingSoon from "@/components/resources/ComingSoon";

export default function Page() {
  return (
    <ComingSoon
      title="Register"
      body="The hierarchy, rollup and search views over your scoped resources — the read API behind them already exists and is being verified; the table itself lands in the next phase of the replatforming plan."
    />
  );
}
