import ComingSoon from "@/components/resources/ComingSoon";

export default function Page() {
  return (
    <ComingSoon
      title="Change log"
      body="Every applied change, who made it, and when — per-item history already exists (open an item's own history once the register lands); a global, filterable browse view is a later phase of the replatforming plan."
    />
  );
}
